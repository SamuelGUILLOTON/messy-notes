"use client"

import { useState } from 'react';
import styles from './save-button-style.module.css'
import { list } from 'postcss';

export const SaveButton = () => {

  return (
    <button className={styles.saveButton}>
        s
    </button>
  );
};
